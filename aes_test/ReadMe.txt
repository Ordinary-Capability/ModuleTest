该测试代码用来在rtt下测试AES加密、解密文件，具体测试时将文件test1.txt（存放明文信息）、test2.txt（存放加密结果）、test3.txt（存放解密结果）拷贝到SD卡，
然后将SD卡挂载到测试板上，然后编译该测试代码，编译时关闭jffs2选项，以免导致SD卡没有挂载而无法打开相关文件。
本代码测试加密100KB文件，每次加密2KB(驱动限定了不超过4095B)，当然其他长度的测均可。